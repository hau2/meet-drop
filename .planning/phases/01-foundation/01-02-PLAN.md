---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/hooks/usePeer.ts
  - src/hooks/usePeer.test.ts
  - .env.development
  - .env.production
  - .env.example
  - src/pages/RoomPage.tsx
  - src/pages/HomePage.tsx
autonomous: false
requirements:
  - PRIV-01

user_setup:
  - service: cloudflare-turn
    why: "TURN relay for users behind corporate firewalls / symmetric NAT"
    env_vars:
      - name: VITE_TURN_USERNAME
        source: "Cloudflare Dashboard -> Realtime -> TURN -> Create key -> copy username"
      - name: VITE_TURN_CREDENTIAL
        source: "Cloudflare Dashboard -> Realtime -> TURN -> Create key -> copy credential"
    dashboard_config:
      - task: "Create a TURN key"
        location: "Cloudflare Dashboard -> Realtime -> TURN -> Create TURN key"
  - service: railway-peerjs
    why: "Self-hosted PeerJS signaling server (always-on, no sleep)"
    env_vars:
      - name: VITE_PEERJS_HOST
        source: "Railway deployment URL (e.g., your-app.railway.app)"
    dashboard_config:
      - task: "Deploy peerjs-server from GitHub repo or template"
        location: "Railway Dashboard -> New Project -> Deploy from GitHub"

must_haves:
  truths:
    - "usePeer hook creates exactly one Peer instance even under React Strict Mode"
    - "PeerJS Peer is configured to use self-hosted signaling server (not 0.peerjs.com)"
    - "PeerJS Peer is configured with TURN server credentials for NAT traversal"
    - "Closing the tab destroys the Peer instance and stops all media tracks"
    - "Environment variables control PeerJS host, port, path, and TURN credentials"
  artifacts:
    - path: "src/hooks/usePeer.ts"
      provides: "PeerJS lifecycle hook with Strict Mode guard and cleanup"
      exports: ["usePeer"]
      min_lines: 30
    - path: ".env.development"
      provides: "Dev environment pointing to localhost:9000 peerjs-server"
      contains: "VITE_PEERJS_HOST=localhost"
    - path: ".env.production"
      provides: "Production environment pointing to Railway peerjs-server"
      contains: "VITE_PEERJS_HOST"
    - path: "src/hooks/usePeer.test.ts"
      provides: "Strict Mode guard verification test"
  key_links:
    - from: "src/hooks/usePeer.ts"
      to: "peerjs"
      via: "new Peer() constructor with config"
      pattern: "new Peer"
    - from: "src/hooks/usePeer.ts"
      to: ".env.*"
      via: "import.meta.env.VITE_PEERJS_*"
      pattern: "import\\.meta\\.env\\.VITE_PEERJS"
    - from: "src/hooks/usePeer.ts"
      to: "beforeunload"
      via: "window.addEventListener cleanup"
      pattern: "beforeunload"
    - from: "src/pages/RoomPage.tsx"
      to: "src/hooks/usePeer.ts"
      via: "usePeer(roomId) call"
      pattern: "usePeer"
---

<objective>
Create the PeerJS lifecycle hook with React Strict Mode guard, configure TURN server credentials, set up environment variables for dev/prod signaling servers, and wire the beforeunload cleanup for zero-persistence. This plan completes the infrastructure foundation.

Purpose: Without this hook pattern, every subsequent phase that touches PeerJS will fight double-initialization bugs, missing TURN relay, and storage leaks on tab close. Getting this right once means Phases 2-5 can focus on features.

Output: A tested usePeer hook, environment configs for dev/prod, and a RoomPage that initializes PeerJS correctly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types and exports the executor needs -->

From src/types/index.ts:
```typescript
export type ConnectionState = 'idle' | 'connecting' | 'connected' | 'disconnected' | 'failed'
```

From src/store/index.ts:
```typescript
export const useCallStore: UseBoundStore<StoreApi<CallStore>>
// CallStore has: connectionState, peerId, setConnectionState, setPeerId, reset
```

From src/pages/RoomPage.tsx:
```typescript
// Skeleton component that reads :id param from wouter useParams
// This plan will wire usePeer(id) into it
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create usePeer hook with Strict Mode guard, TURN config, beforeunload cleanup, env vars, and tests</name>
  <files>src/hooks/usePeer.ts, src/hooks/usePeer.test.ts, .env.development, .env.production, .env.example, src/pages/RoomPage.tsx, src/pages/HomePage.tsx</files>
  <behavior>
    - Test: usePeer creates exactly one Peer instance when useEffect runs twice (Strict Mode simulation)
    - Test: Peer constructor is called with config containing iceServers array that includes a TURN entry
    - Test: cleanup function calls peer.destroy()
    - Test: beforeunload event listener is registered on mount and removed on unmount
  </behavior>
  <action>
    1. Create `.env.development`:
       ```
       VITE_PEERJS_HOST=localhost
       VITE_PEERJS_PORT=9000
       VITE_PEERJS_PATH=/peerjs
       VITE_TURN_USERNAME=openrelayproject
       VITE_TURN_CREDENTIAL=openrelayproject
       ```
       Dev uses Metered Open Relay for TURN (zero setup, 20 GB/month free).

    2. Create `.env.production`:
       ```
       VITE_PEERJS_HOST=REPLACE_WITH_RAILWAY_URL
       VITE_PEERJS_PORT=443
       VITE_PEERJS_PATH=/peerjs
       VITE_TURN_USERNAME=REPLACE_WITH_CLOUDFLARE_CREDENTIAL
       VITE_TURN_CREDENTIAL=REPLACE_WITH_CLOUDFLARE_CREDENTIAL
       ```

    3. Create `.env.example` (same as .env.development but with placeholder comments for documentation).

    4. Create `src/hooks/usePeer.ts`:
       - Import `useEffect`, `useRef`, `useCallback` from React
       - Import `Peer` from `peerjs`
       - Import `useCallStore` from `../store`

       - Define `ICE_SERVERS` array:
         - `{ urls: 'stun:stun.cloudflare.com:3478' }`
         - Metered Open Relay entries for dev (from env vars):
           ```
           {
             urls: [
               'turn:openrelay.metered.ca:80',
               'turn:openrelay.metered.ca:443',
               'turn:openrelay.metered.ca:443?transport=tcp',
             ],
             username: import.meta.env.VITE_TURN_USERNAME,
             credential: import.meta.env.VITE_TURN_CREDENTIAL,
           }
           ```

       - Export `function usePeer(roomId: string)`:
         - `const peerRef = useRef<Peer | null>(null)`
         - `const { setConnectionState, setPeerId } = useCallStore()`

         - `useEffect(() => { ... }, [roomId])`:
           - `if (peerRef.current) return` — Strict Mode guard (CRITICAL)
           - Create `new Peer(roomId, { host, port, path, secure: import.meta.env.PROD, config: { iceServers: ICE_SERVERS } })`
           - `peer.on('open', (id) => { setPeerId(id); setConnectionState('connecting') })`
           - `peer.on('error', (err) => { console.error('PeerJS error:', err); setConnectionState('failed') })`
           - `peer.on('disconnected', () => setConnectionState('disconnected'))`
           - `peerRef.current = peer`
           - Cleanup: `peer.destroy(); peerRef.current = null; setConnectionState('idle'); setPeerId(null)`

         - `useEffect(() => { ... }, [])` — beforeunload handler:
           - `const cleanup = () => { peerRef.current?.destroy(); peerRef.current = null }`
           - `window.addEventListener('beforeunload', cleanup)`
           - Return: `window.removeEventListener('beforeunload', cleanup)`

         - Return `{ peerRef }`

    5. Update `src/pages/RoomPage.tsx`:
       - Import `usePeer` from `../hooks/usePeer`
       - Import `useCallStore` from `../store`
       - Import `useParams` from `wouter` (already used for :id)
       - Call `const { peerRef } = usePeer(id)` where `id` comes from useParams
       - Display connection state from `useCallStore`: `<p>Status: {connectionState}</p>`
       - Display peer ID: `<p>Peer ID: {peerId ?? 'not connected'}</p>`
       - Keep it minimal — this is still a skeleton for Phase 2+

    6. Update `src/pages/HomePage.tsx`:
       - Ensure the store is reset when arriving at HomePage: call `useCallStore().reset()` in a useEffect on mount
       - This ensures navigating back to home cleans up any stale state (PRIV-01 defense in depth)

    7. Write tests in `src/hooks/usePeer.test.ts`:
       - Mock `peerjs` module: `vi.mock('peerjs', () => ({ default: vi.fn() }))` where the mock constructor returns an object with `on`, `destroy`, `id` methods
       - Test "creates exactly one Peer instance under Strict Mode":
         - Render a test component using `usePeer('test-room')` inside `<React.StrictMode>`
         - Assert mock Peer constructor was called exactly once (the `if (peerRef.current) return` guard prevents the second call)
       - Test "passes iceServers config to Peer constructor":
         - Inspect the second argument to the mock Peer constructor
         - Assert `config.iceServers` is an array with length >= 2 (STUN + TURN)
         - Assert at least one entry has a `urls` containing 'turn:'
       - Test "registers beforeunload listener":
         - Spy on `window.addEventListener`
         - Render usePeer hook
         - Assert `addEventListener` was called with 'beforeunload'
       - Test "cleanup calls peer.destroy()":
         - Render and unmount the hook
         - Assert the mock peer's `destroy` was called

    IMPORTANT: The Peer constructor in the mock must return an object with `.on()` that stores callbacks, `.destroy()` that is a vi.fn(), and `.id` property. The mock needs to handle event registration so tests can simulate the 'open' event if needed.

    IMPORTANT: Do NOT use Zustand `persist` middleware anywhere. The store is in-memory only (PRIV-01).
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run</automated>
  </verify>
  <done>usePeer hook creates exactly one Peer under Strict Mode, configures TURN credentials from env vars, registers beforeunload cleanup, and all tests (including Plan 01 tests) pass. Environment files exist for dev and production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 1 foundation in browser</name>
  <action>
    What was built: Complete Phase 1 foundation — Vite project with React 19, Tailwind v4, wouter hash routing, Zustand in-memory store, PeerJS usePeer hook with Strict Mode guard and TURN config, beforeunload cleanup, and full test suite.

    How to verify:
    1. Run `npm run dev` and open http://localhost:5173/meet-drop/#/
    2. Verify the HomePage renders with "MeetDrop" heading and "Create Meeting" button
    3. Click "Create Meeting" — URL should change to /#/room/meet-xxxxxx
    4. On the RoomPage, verify "Status: idle" or connection state is displayed (PeerJS will show an error if no local signaling server is running — that is expected)
    5. Open DevTools > Application tab:
       - localStorage: should be EMPTY
       - sessionStorage: should be EMPTY
       - IndexedDB: should have NO databases
       - Cookies: should be EMPTY
    6. Open DevTools > Console:
       - Verify NO "ID taken" errors (Strict Mode guard is working)
       - A PeerJS connection error to localhost:9000 is expected (no local server running yet)
    7. Run `npm test -- --run` in terminal — all tests should pass
  </action>
  <verify>User confirms app loads, storage is clean, no Strict Mode double-init errors, and tests pass</verify>
  <done>Human approved: app renders correctly, zero storage persistence verified, Strict Mode guard confirmed working</done>
  <resume-signal>Type "approved" if the app loads correctly, storage is clean, and tests pass. Otherwise describe what needs fixing.</resume-signal>
</task>

</tasks>

<verification>
1. `npm test -- --run` — all tests green (room.test, store.test, App.test, usePeer.test)
2. `npx tsc --noEmit` — no TypeScript errors
3. Browser: HomePage renders at /#/, RoomPage at /#/room/:id
4. Browser: DevTools Application tab shows zero storage entries after interaction
5. Browser: Console shows no "ID taken" errors (Strict Mode guard works)
6. `.env.development` exists with Metered Open Relay TURN credentials
7. `.env.production` exists with placeholder Cloudflare/Railway values
</verification>

<success_criteria>
- usePeer hook creates exactly one Peer instance in Strict Mode (verified by test)
- PeerJS configured with TURN credentials from environment variables
- beforeunload handler destroys Peer and cleans up
- Zero storage persistence — no localStorage, IndexedDB, or cookies after any interaction
- All automated tests pass
- Human verification confirms visual correctness and clean storage
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
