---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.node.json
  - src/main.tsx
  - src/index.css
  - src/App.tsx
  - src/pages/HomePage.tsx
  - src/pages/RoomPage.tsx
  - src/types/index.ts
  - src/lib/room.ts
  - src/lib/cn.ts
  - src/store/index.ts
  - src/test/setup.ts
  - src/lib/room.test.ts
  - src/store/index.test.ts
  - src/App.test.tsx
  - index.html
  - .gitignore
autonomous: true
requirements:
  - PRIV-01

must_haves:
  truths:
    - "App loads in the browser from Vite dev server at localhost:5173"
    - "Navigating to #/ renders the HomePage component"
    - "Navigating to #/room/meet-abc123 renders the RoomPage component"
    - "generateRoomId() returns a string matching the pattern meet-[a-z0-9]{6}"
    - "Zustand store writes never appear in localStorage"
    - "All tests pass with npm test -- --run"
  artifacts:
    - path: "src/App.tsx"
      provides: "Hash-based routing with wouter"
      contains: "HashRouter"
    - path: "src/pages/HomePage.tsx"
      provides: "Home page shell"
      min_lines: 5
    - path: "src/pages/RoomPage.tsx"
      provides: "Room page shell"
      min_lines: 5
    - path: "src/lib/room.ts"
      provides: "Room ID generation"
      exports: ["generateRoomId"]
    - path: "src/store/index.ts"
      provides: "Zustand call store with no persist middleware"
      exports: ["useCallStore"]
    - path: "src/types/index.ts"
      provides: "ConnectionState type"
      exports: ["ConnectionState"]
    - path: "vite.config.ts"
      provides: "Vite config with GitHub Pages base, Tailwind v4, jsdom test env"
      contains: "base: '/meet-drop/'"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/HomePage.tsx"
      via: "wouter Route component"
      pattern: "Route.*HomePage"
    - from: "src/App.tsx"
      to: "src/pages/RoomPage.tsx"
      via: "wouter Route component"
      pattern: "Route.*RoomPage"
    - from: "src/store/index.ts"
      to: "zustand"
      via: "create() with NO persist middleware"
      pattern: "create<CallStore>"
    - from: "src/lib/room.ts"
      to: "nanoid"
      via: "customAlphabet import"
      pattern: "customAlphabet"
---

<objective>
Scaffold the MeetDrop project with Vite + React 19 + TypeScript, configure Tailwind v4, wouter hash routing, Zustand in-memory store, room ID generation, and the full test infrastructure. This plan produces a running dev server with navigable routes and passing tests.

Purpose: Establish the project foundation that all subsequent phases build on. The zero-persistence architecture (PRIV-01) is enforced from the first line of code — Zustand with no persist middleware, no localStorage usage anywhere.

Output: A working Vite dev server with two routes, typed store, room ID generation, and green test suite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Vite project and install all dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.app.json, tsconfig.node.json, index.html, src/main.tsx, src/index.css, src/vite-env.d.ts, .gitignore, src/test/setup.ts</files>
  <behavior>
    - Vite dev server starts without errors on `npm run dev`
    - TypeScript compiles without errors on `npx tsc --noEmit`
    - Vitest runs with jsdom environment
  </behavior>
  <action>
    1. Run `npm create vite@latest . -- --template react-ts` in the project root (use `.` to scaffold in-place since the directory exists). If it prompts about existing files, accept overwrite for template files only.

    2. Install runtime dependencies:
       `npm install peerjs wouter zustand nanoid clsx tailwind-merge`

    3. Install Tailwind v4 with Vite plugin:
       `npm install tailwindcss @tailwindcss/vite`

    4. Install dev dependencies:
       `npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom @types/react @types/react-dom`

    5. Replace `vite.config.ts` with:
       - `base: '/meet-drop/'` for GitHub Pages
       - `plugins: [react(), tailwindcss()]`
       - `test` block: `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./src/test/setup.ts']`

    6. Replace `src/index.css` with a single line: `@import "tailwindcss";`

    7. Create `src/test/setup.ts` with: `import '@testing-library/jest-dom'`

    8. Update `src/main.tsx` to render a minimal `<App />` wrapped in `React.StrictMode` (keep the Vite template pattern).

    9. Ensure `.gitignore` includes `node_modules`, `dist`, `.env*.local`.

    10. Delete any Vite template files that are not needed: `src/App.css`, `src/assets/react.svg`, `public/vite.svg`, or any default counter component code.

    IMPORTANT: Do NOT use Tailwind CSS v3 PostCSS config. Tailwind v4 uses the `@tailwindcss/vite` plugin only.
    IMPORTANT: Do NOT install `eslint-plugin-react-hooks` or ESLint config yet — defer linting setup to avoid scope creep.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/meet-drop && npm run dev -- --port 5174 &amp; sleep 3 &amp;&amp; curl -s http://localhost:5174/meet-drop/ | head -5 &amp;&amp; kill %1 2>/dev/null; npx tsc --noEmit</automated>
  </verify>
  <done>Vite dev server starts, serves the app at /meet-drop/, TypeScript compiles cleanly, Vitest config is in place with jsdom environment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create app architecture — routing, types, store, room ID, pages, and tests</name>
  <files>src/App.tsx, src/pages/HomePage.tsx, src/pages/RoomPage.tsx, src/types/index.ts, src/lib/room.ts, src/lib/cn.ts, src/store/index.ts, src/lib/room.test.ts, src/store/index.test.ts, src/App.test.tsx</files>
  <behavior>
    - Test: generateRoomId() returns string matching /^meet-[a-z0-9]{6}$/
    - Test: generateRoomId() produces unique IDs across 100 calls
    - Test: Zustand store useCallStore sets connectionState and peerId without writing to localStorage
    - Test: After store.getState().setConnectionState('connected'), localStorage has no keys
    - Test: App renders HomePage text at route "/"
    - Test: App renders RoomPage text at route "/room/meet-abc123"
  </behavior>
  <action>
    1. Create `src/types/index.ts`:
       - Export `type ConnectionState = 'idle' | 'connecting' | 'connected' | 'disconnected' | 'failed'`

    2. Create `src/lib/room.ts`:
       - Import `customAlphabet` from `nanoid`
       - `const alphabet = '0123456789abcdefghijklmnopqrstuvwxyz'`
       - `const generateId = customAlphabet(alphabet, 6)`
       - Export `function generateRoomId(): string { return \`meet-\${generateId()}\` }`

    3. Create `src/lib/cn.ts`:
       - Import `clsx` (type `ClassValue`) and `twMerge` from `tailwind-merge`
       - Export `function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }`

    4. Create `src/store/index.ts`:
       - Import `create` from `zustand` — NO persist middleware, ever (PRIV-01)
       - Import `ConnectionState` from `../types`
       - Interface `CallStore` with: `connectionState: ConnectionState`, `peerId: string | null`, `setConnectionState`, `setPeerId`, `reset`
       - Export `useCallStore` using bare `create<CallStore>()` — comment: "// No persist — PRIV-01: store lives only in memory"

    5. Create `src/pages/HomePage.tsx`:
       - Functional component rendering a centered container with:
         - `<h1>MeetDrop</h1>` heading
         - `<p>` with tagline "Anonymous video meetings. No sign-up. No trace."
         - A "Create Meeting" button (styled with Tailwind dark theme classes: `bg-zinc-900 text-white`)
         - Button onClick: calls `generateRoomId()` and navigates to `/room/{id}` using wouter's `useLocation`
       - Import `generateRoomId` from `../lib/room`
       - Import `useLocation` from `wouter`

    6. Create `src/pages/RoomPage.tsx`:
       - Functional component that reads `:id` param from wouter `useParams`
       - Renders `<div>Room: {id}</div>` as a placeholder shell
       - This is a skeleton — full room functionality comes in Phase 2+

    7. Replace `src/App.tsx`:
       - Import `HashRouter`, `Route`, `Switch` from `wouter`  (Note: wouter v3 uses `import { useHashLocation } from "wouter/use-hash-location"` and `<Router hook={useHashLocation}>` — check wouter v3 docs. If wouter v3 does NOT export HashRouter, use `<Router hook={useHashLocation}>` instead.)
       - Two routes: `/` -> HomePage, `/room/:id` -> RoomPage
       - Wrap in `<Switch>` for exclusive matching

    8. Write tests (RED first, then GREEN):

       `src/lib/room.test.ts`:
       - Test: result matches `/^meet-[a-z0-9]{6}$/`
       - Test: 100 generated IDs are all unique (Set size === 100)

       `src/store/index.test.ts`:
       - Test: initial state has connectionState 'idle' and peerId null
       - Test: setConnectionState updates state
       - Test: after multiple store operations, `Object.keys(localStorage).length === 0`

       `src/App.test.tsx`:
       - Import `render, screen` from `@testing-library/react`
       - Test: rendering App shows "MeetDrop" heading (verifies HomePage at root)
       - Test: set `window.location.hash = '#/room/meet-abc123'` before render, verify "Room:" text appears

    IMPORTANT: Use wouter v3 API. Check import paths — wouter v3 may require `import { Router } from 'wouter'` and `import { useHashLocation } from 'wouter/use-hash-location'` instead of `HashRouter`. The research doc references v3.9.0.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run</automated>
  </verify>
  <done>All 7+ tests pass. App renders HomePage at #/ and RoomPage at #/room/:id. generateRoomId() produces meet-xxxxxx format. Zustand store never writes to localStorage. cn() utility available for Tailwind class merging.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npx tsc --noEmit` compiles cleanly
3. `npm test -- --run` — all tests green
4. Browser: `http://localhost:5173/meet-drop/#/` shows MeetDrop heading
5. Browser: clicking "Create Meeting" navigates to `/#/room/meet-xxxxxx`
6. Browser: DevTools > Application > localStorage is empty after all interactions
</verification>

<success_criteria>
- Vite dev server serves the app at /meet-drop/
- Two routes work: #/ (HomePage) and #/room/:id (RoomPage)
- generateRoomId() returns meet-[a-z0-9]{6} format
- Zustand store has zero persistence — no localStorage, no IndexedDB, no cookies
- All unit and smoke tests pass
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
