---
phase: 02-media-lobby
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/HomePage.tsx
  - src/pages/HomePage.test.tsx
  - src/components/CopyLinkButton.tsx
  - src/components/CopyLinkButton.test.tsx
  - src/components/MediaControls.tsx
  - src/components/SelfViewOverlay.tsx
  - src/components/SelfViewOverlay.test.tsx
  - src/pages/RoomPage.tsx
autonomous: false
requirements: [CONN-01, CONN-02, CONN-03, AV-05, UX-01]

must_haves:
  truths:
    - "User creates a room and the lobby shows their camera preview"
    - "User can copy the meeting link to clipboard with one click"
    - "User can join by entering a Room ID manually on the home page"
    - "User sees their own video as a mirrored PiP overlay"
    - "Mic and camera toggle buttons visually reflect on/off state"
    - "Lobby layout works on 375px mobile and 1280px desktop without horizontal scroll"
  artifacts:
    - path: "src/pages/HomePage.tsx"
      provides: "Manual Room ID join input + Create Meeting button"
      contains: "handleJoin"
    - path: "src/components/CopyLinkButton.tsx"
      provides: "One-click clipboard copy with 'Copied!' feedback"
      exports: ["CopyLinkButton"]
    - path: "src/components/MediaControls.tsx"
      provides: "Mic and camera toggle icon buttons"
      exports: ["MediaControls"]
    - path: "src/components/SelfViewOverlay.tsx"
      provides: "PiP self-view with mirrored video"
      exports: ["SelfViewOverlay"]
    - path: "src/pages/RoomPage.tsx"
      provides: "Full lobby layout with video preview, controls, copy link, self-view overlay"
      contains: "useMedia"
  key_links:
    - from: "src/pages/RoomPage.tsx"
      to: "src/hooks/useMedia.ts"
      via: "useMedia() hook call"
      pattern: "useMedia\\(\\)"
    - from: "src/pages/RoomPage.tsx"
      to: "src/components/VideoPreview.tsx"
      via: "VideoPreview stream={streamRef.current}"
      pattern: "VideoPreview"
    - from: "src/pages/RoomPage.tsx"
      to: "src/components/MediaControls.tsx"
      via: "MediaControls with toggleMic/toggleCamera props"
      pattern: "MediaControls"
    - from: "src/pages/RoomPage.tsx"
      to: "src/components/CopyLinkButton.tsx"
      via: "CopyLinkButton url={roomLink}"
      pattern: "CopyLinkButton"
    - from: "src/pages/HomePage.tsx"
      to: "wouter useLocation"
      via: "setLocation(/room/{id}) on manual join"
      pattern: "setLocation"
    - from: "src/components/CopyLinkButton.tsx"
      to: "navigator.clipboard.writeText"
      via: "onClick handler"
      pattern: "clipboard\\.writeText"
---

<objective>
Build the complete lobby UI: HomePage manual join input, CopyLinkButton for sharing, MediaControls for mic/camera toggles, SelfViewOverlay for PiP self-view, and the RoomPage lobby layout that assembles everything. This is the user-facing shell around the media primitives from Plan 01.

Purpose: Users need to create rooms, share links, preview their camera, toggle mic/camera, and join via Room ID -- all before any peer connection exists.
Output: Complete pre-join lobby experience with responsive layout.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-media-lobby/02-RESEARCH.md
@.planning/phases/02-media-lobby/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (created in previous plan) -->

From src/hooks/useMedia.ts:
```typescript
export type MediaError = 'not-allowed' | 'not-found' | 'not-readable' | 'unknown'
export function useMedia(): {
  streamRef: React.RefObject<MediaStream | null>
  error: MediaError | null
  isLoading: boolean
  toggleMic: () => void
  toggleCamera: () => void
}
```

From src/components/VideoPreview.tsx:
```typescript
export function VideoPreview(props: {
  stream: MediaStream | null
  mirror?: boolean
  className?: string
}): JSX.Element
```

From src/store/index.ts:
```typescript
interface CallStore {
  connectionState: ConnectionState
  peerId: string | null
  isMicOn: boolean
  isCameraOn: boolean
  setConnectionState: (state: ConnectionState) => void
  setPeerId: (id: string | null) => void
  setMicOn: (on: boolean) => void
  setCameraOn: (on: boolean) => void
  reset: () => void
}
```

From src/lib/room.ts:
```typescript
export function generateRoomId(): string  // returns 'meet-xxxxxx'
```

From src/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HomePage manual join input and lobby UI components</name>
  <files>src/pages/HomePage.tsx, src/pages/HomePage.test.tsx, src/components/CopyLinkButton.tsx, src/components/CopyLinkButton.test.tsx, src/components/MediaControls.tsx, src/components/SelfViewOverlay.tsx, src/components/SelfViewOverlay.test.tsx</files>
  <behavior>
    - Test 1 (HomePage): typing "meet-abc123" in join input and clicking Join navigates to /room/meet-abc123
    - Test 2 (HomePage): pasting full URL "https://example.com/#/room/meet-abc123" extracts meet-abc123 and navigates
    - Test 3 (HomePage): empty input does not navigate
    - Test 4 (HomePage): invalid input (no meet-xxxxxx pattern) does not navigate
    - Test 5 (CopyLinkButton): clicking calls navigator.clipboard.writeText with the url prop
    - Test 6 (CopyLinkButton): after click, button text changes to "Copied!"
    - Test 7 (SelfViewOverlay): renders a VideoPreview with mirror=true
    - Test 8 (SelfViewOverlay): has absolute positioning classes for PiP corner placement
  </behavior>
  <action>
**Step 1 -- Write tests (RED):**

Create `src/pages/HomePage.test.tsx`:
- Mock `wouter` useLocation to capture setLocation calls
- Render HomePage, find input by placeholder "Enter Room ID", type values, find Join button, click
- Assert `setLocation` called with `/room/meet-abc123` for valid input
- Assert `setLocation` NOT called for empty or invalid input

Create `src/components/CopyLinkButton.test.tsx`:
- Mock `navigator.clipboard.writeText` via Object.defineProperty
- Render `<CopyLinkButton url="https://example.com/#/room/meet-abc123" />`
- Click button, assert `writeText` called with correct URL
- After click, assert button text is "Copied!"

Create `src/components/SelfViewOverlay.test.tsx`:
- Render `<SelfViewOverlay stream={null} />`
- Assert container has `absolute` class
- Assert the inner video area renders (VideoPreview exists in DOM)

**Step 2 -- Implement components (GREEN):**

**HomePage.tsx** -- Add manual join input below the Create Meeting button:
- State: `const [joinId, setJoinId] = useState('')`
- `handleJoin`: extract `meet-[0-9a-z]{6}` pattern from input, call `setLocation('/room/${match[0]}')`. Support both raw room IDs and full URLs pasted in.
- JSX: `<input>` with placeholder "Enter Room ID (meet-xxxxxx)" + "Join" button
- Keep existing Create Meeting button and reset logic

**CopyLinkButton.tsx** -- Follow RESEARCH.md Pattern 4 exactly:
- Props: `{ url: string }`
- State: `const [copied, setCopied] = useState(false)`
- `handleCopy`: try `navigator.clipboard.writeText(url)`, catch with execCommand fallback, set copied=true, setTimeout 2s reset
- Button text: `copied ? 'Copied!' : 'Copy Link'`
- Style: subtle zinc button matching app theme

**MediaControls.tsx** -- Mic and camera toggle buttons:
- Props: `{ isMicOn: boolean, isCameraOn: boolean, onToggleMic: () => void, onToggleCamera: () => void }`
- Two icon buttons in a flex row with gap
- Mic button: show mic-on/mic-off text or SVG icon, background changes for on/off state (zinc-700 for on, red-600 for off)
- Camera button: same pattern
- Use `cn()` for conditional class merging
- Use accessible labels: `aria-label="Toggle microphone"`, `aria-label="Toggle camera"`

**SelfViewOverlay.tsx** -- Follow RESEARCH.md Pattern 3:
- Props: `{ stream: MediaStream | null }`
- Wrapper div: `absolute bottom-4 right-4 w-32 md:w-40 aspect-video rounded-lg overflow-hidden shadow-lg ring-1 ring-white/10 z-10`
- Inner: `<VideoPreview stream={stream} mirror className="w-full h-full object-cover" />`
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run src/pages/HomePage.test.tsx src/components/CopyLinkButton.test.tsx src/components/SelfViewOverlay.test.tsx</automated>
  </verify>
  <done>HomePage accepts manual Room ID input and navigates correctly. CopyLinkButton copies URL to clipboard with feedback. MediaControls renders toggle buttons. SelfViewOverlay renders PiP self-view. All tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Assemble RoomPage lobby layout with responsive design</name>
  <files>src/pages/RoomPage.tsx</files>
  <action>
Rewrite `RoomPage.tsx` to render a full lobby layout. This is the assembly task that wires all Plan 01 and Plan 02 components together.

**Layout structure (mobile-first, per RESEARCH.md Pattern 7):**

Outer wrapper: `flex flex-col md:flex-row gap-4 p-4 w-full max-w-4xl mx-auto min-h-screen items-center justify-center`

Left column (video area): `relative w-full md:flex-1 aspect-video bg-zinc-900 rounded-xl overflow-hidden`
- Contains `<VideoPreview stream={streamRef.current} mirror className="w-full h-full object-cover" />` as the main preview
- Shows loading state while `isLoading` is true (spinner or "Requesting camera..." text)
- Shows error state if `error` is set (user-friendly message per error type: "Camera access denied" for not-allowed, "No camera found" for not-found, etc.)

Right column (controls sidebar): `flex flex-col gap-3 w-full md:w-64`
- Room ID display: `<p>Room: {id}</p>`
- `<MediaControls isMicOn={isMicOn} isCameraOn={isCameraOn} onToggleMic={toggleMic} onToggleCamera={toggleCamera} />`
- `<CopyLinkButton url={roomLink} />` where `roomLink` is constructed as `${window.location.origin}${import.meta.env.BASE_URL.replace(/\/$/, '')}/#/room/${id}`
- "Join Meeting" button (for Phase 3 -- disabled placeholder for now with text "Waiting for peer..." since Phase 3 implements the actual call connection). Style: `bg-emerald-600 hover:bg-emerald-500 text-white px-6 py-3 rounded-lg w-full`

**SelfViewOverlay:** Render `<SelfViewOverlay stream={streamRef.current} />` inside the video area div (it positions itself absolutely)

**Hook calls:**
- `const { id } = useParams<{ id: string }>()`
- `const { peerRef: _peerRef } = usePeer(id ?? '')` (keep existing -- peer registers on signaling server)
- `const { streamRef, error, isLoading, toggleMic, toggleCamera } = useMedia()`
- `const { isMicOn, isCameraOn } = useCallStore()`

**Key details:**
- Do NOT remove usePeer call -- the peer needs to register on the signaling server while in the lobby so Phase 3 can use it
- Use `streamRef.current` to pass stream to components. Since streamRef is a ref (not state), force a re-render after stream is acquired. The `isLoading` state change from useMedia already triggers this.
- Dark theme: all backgrounds use zinc-900/zinc-800/black, text is white/zinc-400
- No horizontal scroll at 375px: use w-full, no fixed pixel widths
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run && npx tsc --noEmit</automated>
  </verify>
  <done>RoomPage renders full lobby layout with video preview, media controls, copy link button, self-view overlay, room ID display, and join placeholder. Responsive layout uses mobile-first Tailwind. All tests pass, zero TS errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete lobby experience</name>
  <files></files>
  <action>Human verifies the complete lobby experience works end-to-end.</action>
  <what-built>Complete lobby experience: camera preview, mic/camera toggles, copy link, manual join, responsive layout, PiP self-view</what-built>
  <how-to-verify>
    1. Run `cd /Users/avepoint_admin/Downloads/meet-drop && npm run dev` and open http://localhost:5173/meet-drop/
    2. On the home page, verify "Create Meeting" button works (navigates to lobby)
    3. Go back home. Type "meet-abc123" in the join input and click Join -- verify it navigates to /room/meet-abc123
    4. In the lobby, verify camera preview appears (browser will prompt for camera permission -- allow it)
    5. Click mic toggle -- verify it visually changes to "off" state
    6. Click camera toggle -- verify it visually changes to "off" state (video may show black)
    7. Click "Copy Link" -- paste somewhere and verify the URL is correct (contains the room ID)
    8. Verify "Copied!" feedback appears briefly after clicking
    9. Open Chrome DevTools, toggle device emulation to 375px width -- verify no horizontal scrollbar, layout stacks vertically
    10. Toggle to 1280px width -- verify two-column layout (video left, controls right)
    11. Verify the small PiP self-view overlay appears in bottom-right corner of the video area
    12. Navigate back to home page -- verify camera indicator light turns off (stream released)
  </how-to-verify>
  <verify>Human inspection</verify>
  <done>All 12 verification steps pass. Lobby is functional, responsive, and camera is properly released on navigation.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run && npx tsc --noEmit
```
Full test suite green (Phase 1 + Phase 2 tests). Zero TypeScript errors. Manual verification of responsive layout at 375px and 1280px.
</verification>

<success_criteria>
- HomePage has manual join input that accepts room IDs and full URLs
- CopyLinkButton copies meeting URL to clipboard with "Copied!" feedback
- MediaControls shows mic/camera toggle buttons with visual on/off state
- SelfViewOverlay shows mirrored PiP self-view in corner
- RoomPage lobby layout is responsive: single column on mobile, two columns on desktop
- No horizontal scroll at 375px viewport width
- Camera indicator turns off when navigating away from lobby
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-lobby/02-02-SUMMARY.md`
</output>
