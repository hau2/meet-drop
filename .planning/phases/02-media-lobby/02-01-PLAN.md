---
phase: 02-media-lobby
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/store/index.ts
  - src/hooks/useMedia.ts
  - src/hooks/useMedia.test.ts
  - src/components/VideoPreview.tsx
  - src/components/VideoPreview.test.tsx
autonomous: true
requirements: [AV-02, AV-03, AV-04]

must_haves:
  truths:
    - "getUserMedia is called on hook mount and tracks are stopped on unmount"
    - "toggleMic flips audio track enabled state and syncs Zustand store"
    - "toggleCamera flips video track enabled state and syncs Zustand store"
    - "VideoPreview assigns a MediaStream to a video element via srcObject"
    - "Async getUserMedia cancellation prevents state updates after unmount"
  artifacts:
    - path: "src/hooks/useMedia.ts"
      provides: "Media stream lifecycle hook with toggle functions"
      exports: ["useMedia", "MediaError"]
    - path: "src/components/VideoPreview.tsx"
      provides: "Video element that displays a MediaStream"
      exports: ["VideoPreview"]
    - path: "src/store/index.ts"
      provides: "Extended Zustand store with isMicOn, isCameraOn, setMicOn, setCameraOn"
      contains: "isMicOn"
    - path: "src/types/index.ts"
      provides: "MediaError type"
      contains: "MediaError"
  key_links:
    - from: "src/hooks/useMedia.ts"
      to: "navigator.mediaDevices.getUserMedia"
      via: "useEffect on mount"
      pattern: "getUserMedia"
    - from: "src/hooks/useMedia.ts"
      to: "src/store/index.ts"
      via: "useCallStore for isMicOn/isCameraOn sync"
      pattern: "useCallStore"
    - from: "src/components/VideoPreview.tsx"
      to: "HTMLVideoElement.srcObject"
      via: "useRef + useEffect"
      pattern: "srcObject"
---

<objective>
Build the media acquisition primitives: a `useMedia` hook that manages getUserMedia lifecycle with track toggles, and a `VideoPreview` component for displaying live MediaStream. Extend the Zustand store and types to support media state.

Purpose: These are the building blocks every lobby and call UI depends on. No UI can display camera or toggle mic/camera without them.
Output: useMedia hook, VideoPreview component, extended store, and passing test suite.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-media-lobby/02-RESEARCH.md
@.planning/phases/02-media-lobby/02-VALIDATION.md

<interfaces>
<!-- Existing interfaces from Phase 1 that this plan extends -->

From src/types/index.ts:
```typescript
export type ConnectionState = 'idle' | 'connecting' | 'connected' | 'disconnected' | 'failed'
```

From src/store/index.ts:
```typescript
interface CallStore {
  connectionState: ConnectionState
  peerId: string | null
  setConnectionState: (state: ConnectionState) => void
  setPeerId: (id: string | null) => void
  reset: () => void
}
export const useCallStore = create<CallStore>(...)
```

From src/test/setup.ts:
```typescript
import '@testing-library/jest-dom'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend store and types, build useMedia hook with TDD</name>
  <files>src/types/index.ts, src/store/index.ts, src/hooks/useMedia.ts, src/hooks/useMedia.test.ts</files>
  <behavior>
    - Test 1: useMedia calls getUserMedia on mount with video+audio constraints
    - Test 2: useMedia stops all tracks on unmount (track.stop() called)
    - Test 3: useMedia sets cancelled flag and stops orphaned stream if unmount happens before getUserMedia resolves
    - Test 4: toggleMic sets audioTrack.enabled to false and store isMicOn to false
    - Test 5: toggleMic sets audioTrack.enabled back to true and store isMicOn to true
    - Test 6: toggleCamera sets videoTrack.enabled to false and store isCameraOn to false
    - Test 7: getUserMedia NotAllowedError sets error to 'not-allowed'
    - Test 8: getUserMedia NotFoundError sets error to 'not-found'
    - Test 9: Store reset() clears isMicOn and isCameraOn back to true
  </behavior>
  <action>
**Step 1 — Extend types (src/types/index.ts):**
Add `MediaError` type: `export type MediaError = 'not-allowed' | 'not-found' | 'not-readable' | 'unknown'`
Keep existing `ConnectionState` unchanged.

**Step 2 — Extend store (src/store/index.ts):**
Add to CallStore interface: `isMicOn: boolean`, `isCameraOn: boolean`, `setMicOn: (on: boolean) => void`, `setCameraOn: (on: boolean) => void`.
Defaults: `isMicOn: true`, `isCameraOn: true`.
Update `reset()` to also reset `isMicOn: true, isCameraOn: true`.

**Step 3 — Write tests (RED):**
Create `src/hooks/useMedia.test.ts`. Mock `navigator.mediaDevices.getUserMedia` using the pattern from RESEARCH.md — define mock tracks with `enabled: true` and `stop: vi.fn()`, mock stream with `getTracks()`, `getAudioTracks()`, `getVideoTracks()`. Use `renderHook` from `@testing-library/react`. Use `act()` and `waitFor()` for async getUserMedia resolution.

Mock setup at top of test file:
```typescript
const mockAudioTrack = { kind: 'audio', enabled: true, stop: vi.fn() }
const mockVideoTrack = { kind: 'video', enabled: true, stop: vi.fn() }
const mockStream = {
  getTracks: () => [mockAudioTrack, mockVideoTrack],
  getAudioTracks: () => [mockAudioTrack],
  getVideoTracks: () => [mockVideoTrack],
}
```
Define `navigator.mediaDevices` via `Object.defineProperty` before tests.

**Step 4 — Implement useMedia (GREEN):**
Create `src/hooks/useMedia.ts` following the exact pattern from RESEARCH.md Pattern 1. Key points:
- `streamRef = useRef<MediaStream | null>(null)`
- `useEffect` with `cancelled` flag for async safety
- `getUserMedia({ video: { width: { ideal: 1280 }, height: { ideal: 720 }, facingMode: 'user' }, audio: { echoCancellation: true, noiseSuppression: true, autoGainControl: true } })`
- Error classification using DOMException.name mapping
- Cleanup: `streamRef.current?.getTracks().forEach(t => t.stop())`
- `toggleMic` and `toggleCamera` use `useCallback`, flip `track.enabled`, call `setMicOn`/`setCameraOn`
- On new stream acquired, sync store: `setMicOn(true)`, `setCameraOn(true)`
- Return `{ streamRef, error, isLoading, toggleMic, toggleCamera }`
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run src/hooks/useMedia.test.ts</automated>
  </verify>
  <done>useMedia hook acquires getUserMedia on mount, stops tracks on unmount, toggleMic/toggleCamera flip track.enabled and sync store, error states classified. All tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build VideoPreview component with TDD</name>
  <files>src/components/VideoPreview.tsx, src/components/VideoPreview.test.tsx</files>
  <behavior>
    - Test 1: VideoPreview assigns stream to video element srcObject
    - Test 2: VideoPreview renders with muted, autoPlay, playsInline attributes
    - Test 3: VideoPreview applies -scale-x-100 class when mirror=true
    - Test 4: VideoPreview does not apply -scale-x-100 when mirror=false
    - Test 5: VideoPreview updates srcObject when stream prop changes
  </behavior>
  <action>
**Step 1 — Write tests (RED):**
Create `src/components/VideoPreview.test.tsx`. Import `render` from `@testing-library/react`. Create a mock MediaStream object (plain object is sufficient for srcObject assignment test). Query `<video>` element, assert `srcObject === mockStream`, assert `muted`, `autoPlay`, `playsInline` attributes present. For mirror test, check className contains `-scale-x-100`.

Note: jsdom does not implement `srcObject` natively. The test should verify the useEffect ran by checking the ref assignment. Use `Object.defineProperty(HTMLVideoElement.prototype, 'srcObject', { set: vi.fn(), get: vi.fn() })` or assign directly and read back.

**Step 2 — Implement VideoPreview (GREEN):**
Create `src/components/VideoPreview.tsx` following RESEARCH.md Pattern 2 exactly:
```typescript
interface VideoPreviewProps {
  stream: MediaStream | null
  mirror?: boolean
  className?: string
}
```
- `useRef<HTMLVideoElement>(null)` + `useEffect` assigns `videoRef.current.srcObject = stream`
- JSX: `<video ref={videoRef} autoPlay playsInline muted className={...} />`
- Mirror: conditionally add `-scale-x-100` class
- Use `cn()` from `src/lib/cn.ts` for class merging
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run src/components/VideoPreview.test.tsx</automated>
  </verify>
  <done>VideoPreview component assigns MediaStream to srcObject via ref, renders with muted/autoPlay/playsInline, mirrors with -scale-x-100. All tests green.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run && npx tsc --noEmit
```
All existing Phase 1 tests still pass. New useMedia and VideoPreview tests pass. Zero TypeScript errors.
</verification>

<success_criteria>
- useMedia hook acquires camera+mic on mount, stops all tracks on unmount
- toggleMic/toggleCamera flip track.enabled and sync Zustand store
- getUserMedia errors classified into MediaError type
- VideoPreview assigns stream to srcObject, renders muted/autoPlay/playsInline
- Zustand store has isMicOn, isCameraOn, setMicOn, setCameraOn
- Full test suite green (Phase 1 + Phase 2 new tests)
</success_criteria>

<output>
After completion, create `.planning/phases/02-media-lobby/02-01-SUMMARY.md`
</output>
