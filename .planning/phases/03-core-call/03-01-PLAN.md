---
phase: 03-core-call
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/store/index.ts
  - src/lib/sounds.ts
  - src/lib/sounds.test.ts
  - src/components/ConnectionStatus.tsx
  - src/components/ConnectionStatus.test.tsx
  - src/components/MeetingEnded.tsx
  - src/components/MeetingEnded.test.tsx
autonomous: true
requirements: [CONN-04, UX-02, UX-04]

must_haves:
  truths:
    - "ConnectionStatus component renders correct label for each ConnectionState value"
    - "MeetingEnded screen shows heading, New Meeting button, and Return Home button"
    - "MeetingEnded navigates to / on Return Home and to a new room on New Meeting"
    - "playJoinSound creates ascending two-tone via Web Audio API"
    - "playLeaveSound creates descending two-tone via Web Audio API"
    - "Store has callEnded boolean and wasConnected boolean with reset clearing both"
  artifacts:
    - path: "src/store/index.ts"
      provides: "Extended CallStore with callEnded, wasConnected, setCallEnded, setWasConnected"
      contains: "callEnded"
    - path: "src/lib/sounds.ts"
      provides: "playJoinSound and playLeaveSound functions"
      exports: ["playJoinSound", "playLeaveSound"]
    - path: "src/components/ConnectionStatus.tsx"
      provides: "Badge component showing connection state label"
      exports: ["ConnectionStatus"]
    - path: "src/components/MeetingEnded.tsx"
      provides: "Meeting ended screen with navigation actions"
      exports: ["MeetingEnded"]
  key_links:
    - from: "src/components/ConnectionStatus.tsx"
      to: "src/types/index.ts"
      via: "ConnectionState type import"
      pattern: "import.*ConnectionState.*from"
    - from: "src/components/MeetingEnded.tsx"
      to: "src/lib/room.ts"
      via: "generateRoomId for New Meeting"
      pattern: "generateRoomId"
---

<objective>
Create the building-block components and utilities for the core call feature: store extension with call-ended state, sound notification functions, ConnectionStatus badge, and MeetingEnded screen.

Purpose: These are independent units that Plan 02 will wire into the useCall hook and RoomPage. Building them first with tests ensures the call wiring plan has stable contracts to consume.
Output: Four new files (sounds.ts, ConnectionStatus.tsx, MeetingEnded.tsx) plus store/types extensions, all with passing tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-call/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/index.ts:
```typescript
export type ConnectionState = 'idle' | 'connecting' | 'connected' | 'disconnected' | 'failed'
export type MediaError = 'not-allowed' | 'not-found' | 'not-readable' | 'unknown'
```

From src/store/index.ts:
```typescript
interface CallStore {
  connectionState: ConnectionState
  peerId: string | null
  isMicOn: boolean
  isCameraOn: boolean
  setConnectionState: (state: ConnectionState) => void
  setPeerId: (id: string | null) => void
  setMicOn: (on: boolean) => void
  setCameraOn: (on: boolean) => void
  reset: () => void
}
export const useCallStore = create<CallStore>(...)
```

From src/lib/room.ts:
```typescript
export function generateRoomId(): string  // returns "meet-xxxxxx"
```

Available UI components: Badge (src/components/ui/badge.tsx), Button (src/components/ui/button.tsx), Card, Input
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend store, create sounds utility, and ConnectionStatus component</name>
  <files>src/types/index.ts, src/store/index.ts, src/lib/sounds.ts, src/lib/sounds.test.ts, src/components/ConnectionStatus.tsx, src/components/ConnectionStatus.test.tsx</files>
  <behavior>
    - sounds.test.ts: playJoinSound() creates an AudioContext if none exists, calls createOscillator twice, calls start on both oscillators
    - sounds.test.ts: playLeaveSound() reuses existing AudioContext, creates two oscillators with descending frequencies
    - sounds.test.ts: playJoinSound() calls audioCtx.resume() if state is 'suspended'
    - ConnectionStatus.test.tsx: renders "Connecting" text when state is 'connecting'
    - ConnectionStatus.test.tsx: renders "Connected" text when state is 'connected'
    - ConnectionStatus.test.tsx: renders "Disconnected" text when state is 'disconnected'
    - ConnectionStatus.test.tsx: renders "Connection Failed" text when state is 'failed'
    - ConnectionStatus.test.tsx: renders "Waiting" text when state is 'idle'
    - store/index.test.ts: callEnded defaults to false, setCallEnded(true) updates it, reset() clears it back to false
    - store/index.test.ts: wasConnected defaults to false, setWasConnected(true) updates it, reset() clears it back to false
  </behavior>
  <action>
    1. Update src/types/index.ts — no changes needed, ConnectionState already has all 5 values.

    2. Extend src/store/index.ts — add `callEnded: boolean`, `wasConnected: boolean`, `setCallEnded`, `setWasConnected` to the CallStore interface. Add defaults (false) and setters to create(). Update reset() to include `callEnded: false, wasConnected: false`. The `wasConnected` flag distinguishes "call ended after connection" from "call failed to connect" (Pitfall 3 from research).

    3. Create src/lib/sounds.ts — Web Audio API synthesized tones per research Pattern 7. Singleton AudioContext (lazy init). `playJoinSound()`: ascending C5→G5 (523Hz→784Hz). `playLeaveSound()`: descending G5→C5 (784Hz→523Hz). Each tone: OscillatorNode + GainNode, 0.12 volume, exponential ramp to 0.001. Guard: resume() if AudioContext state is 'suspended'.

    4. Create src/lib/sounds.test.ts — mock global AudioContext with createOscillator/createGain returning mock objects. Assert oscillator.start called, frequency.setValueAtTime called with correct frequencies. Test that suspended context triggers resume().

    5. Create src/components/ConnectionStatus.tsx — accepts `{ state: ConnectionState }` prop. Maps state to label via const record: idle→"Waiting", connecting→"Connecting", connected→"Connected", disconnected→"Disconnected", failed→"Connection Failed". Maps state to Badge variant: connected→"default", disconnected/failed→"destructive", idle/connecting→"secondary". Render shadcn Badge.

    6. Create src/components/ConnectionStatus.test.tsx — render ConnectionStatus with each state value, assert correct text content is visible.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run src/lib/sounds.test.ts src/components/ConnectionStatus.test.tsx src/store/index.test.ts</automated>
  </verify>
  <done>Store has callEnded + wasConnected with setters and reset. playJoinSound/playLeaveSound export from sounds.ts. ConnectionStatus renders correct label for all 5 states. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MeetingEnded component with navigation</name>
  <files>src/components/MeetingEnded.tsx, src/components/MeetingEnded.test.tsx</files>
  <behavior>
    - MeetingEnded.test.tsx: renders "Meeting Ended" heading
    - MeetingEnded.test.tsx: renders "New Meeting" button and "Return Home" button
    - MeetingEnded.test.tsx: clicking "Return Home" calls setLocation with "/"
    - MeetingEnded.test.tsx: clicking "New Meeting" calls setLocation with a path matching /\/room\/meet-[0-9a-z]{6}/
    - MeetingEnded.test.tsx: clicking "New Meeting" calls store reset() before navigation
  </behavior>
  <action>
    Create src/components/MeetingEnded.tsx per research Pattern 6:
    - Import useLocation from wouter, generateRoomId from ../lib/room, Button from ui/button, useCallStore from ../store
    - Render centered full-screen layout: "Meeting Ended" h1, "The call has ended." subtitle in text-muted-foreground
    - "New Meeting" button (default variant): calls useCallStore.getState().reset(), then setLocation(`/room/${generateRoomId()}`)
    - "Return Home" button (outline variant): calls useCallStore.getState().reset(), then setLocation('/')
    - CRITICAL: reset store BEFORE navigation to prevent new room showing MeetingEnded immediately (Pitfall 5 from research)

    Create src/components/MeetingEnded.test.tsx:
    - Mock wouter's useLocation to capture setLocation calls
    - Mock store reset via vi.spyOn on useCallStore.getState().reset
    - Render MeetingEnded, assert heading text, button text
    - Click "Return Home" → assert reset() called, then setLocation('/') called
    - Click "New Meeting" → assert reset() called, then setLocation called with room path pattern
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run src/components/MeetingEnded.test.tsx</automated>
  </verify>
  <done>MeetingEnded component renders heading and two buttons. Return Home navigates to /. New Meeting generates room ID and navigates to /room/{id}. Store reset() is called before each navigation. All tests pass.</done>
</task>

</tasks>

<verification>
All Phase 3 Plan 01 tests pass:
```bash
cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run src/lib/sounds.test.ts src/components/ConnectionStatus.test.tsx src/components/MeetingEnded.test.tsx src/store/index.test.ts
```
Full existing test suite still passes:
```bash
cd /Users/avepoint_admin/Downloads/meet-drop && npm test -- --run
```
</verification>

<success_criteria>
- ConnectionStatus renders correct label and variant for all 5 ConnectionState values
- MeetingEnded renders heading, two buttons, navigates correctly, resets store
- playJoinSound/playLeaveSound synthesize distinct tones via Web Audio API
- Store extended with callEnded + wasConnected booleans, reset clears both
- All new tests pass, all existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-call/03-01-SUMMARY.md`
</output>
