---
phase: 04-encrypted-chat
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/ChatPanel.tsx
  - src/components/ChatPanel.test.tsx
  - src/components/CallView.tsx
  - src/pages/RoomPage.tsx
autonomous: false
requirements: [CHAT-01]

must_haves:
  truths:
    - "User can open a chat panel during a call with a toggle button"
    - "User can type a message and send it by pressing Enter or clicking Send"
    - "Sent messages appear in the chat panel with local/remote distinction"
    - "Remote peer receives and displays the message"
    - "Chat panel auto-scrolls to newest message"
    - "Chat history disappears when tab closes — no persistence"
  artifacts:
    - path: "src/components/ChatPanel.tsx"
      provides: "Collapsible chat UI with message list and input"
      min_lines: 40
    - path: "src/components/ChatPanel.test.tsx"
      provides: "Render tests for ChatPanel"
    - path: "src/components/CallView.tsx"
      provides: "Updated CallView with chat toggle button and ChatPanel"
      contains: "ChatPanel"
    - path: "src/pages/RoomPage.tsx"
      provides: "Updated RoomPage passing useChat return values to CallView"
      contains: "useChat"
  key_links:
    - from: "src/components/CallView.tsx"
      to: "src/components/ChatPanel.tsx"
      via: "conditional render when isChatOpen"
      pattern: "isChatOpen.*ChatPanel"
    - from: "src/pages/RoomPage.tsx"
      to: "src/hooks/useChat.ts"
      via: "useChat(peerRef, roomId) call"
      pattern: "useChat"
    - from: "src/components/ChatPanel.tsx"
      to: "src/store/index.ts"
      via: "reads messages from useCallStore"
      pattern: "useCallStore.*messages"
---

<objective>
Build the ChatPanel UI and wire it into the active call view.

Purpose: Users in a connected call can open a chat panel, type messages, and see encrypted messages from the remote peer displayed in real time. This completes the CHAT-01 requirement with a visible, functional UI.

Output: ChatPanel component, updated CallView with chat toggle, RoomPage wiring, and human verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-encrypted-chat/04-RESEARCH.md
@.planning/phases/04-encrypted-chat/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/hooks/useChat.ts (created in Plan 01):
```typescript
export function useChat(
  peerRef: RefObject<Peer | null>,
  roomId: string
): { sendMessage: (text: string) => Promise<void>; isReady: boolean }
```

From src/store/index.ts (extended in Plan 01):
```typescript
interface CallStore {
  // ... existing fields ...
  messages: ChatMessage[]
  isChatOpen: boolean
  addMessage: (msg: ChatMessage) => void
  setChatOpen: (open: boolean) => void
  reset: () => void  // clears messages and isChatOpen too
}
```

From src/types/index.ts (extended in Plan 01):
```typescript
export interface ChatMessage {
  from: 'local' | 'remote'
  text: string
  timestamp: number
}
```

From src/components/CallView.tsx (current — will be modified):
```typescript
interface CallViewProps {
  remoteStreamRef: RefObject<MediaStream | null>
  localStreamRef: RefObject<MediaStream | null>
  onHangUp: () => void
  toggleMic: () => void
  toggleCamera: () => void
}
```

From src/pages/RoomPage.tsx (current — will be modified):
```typescript
const { peerRef } = usePeer(id ?? '')
const { remoteStreamRef, hangUp } = useCall(peerRef, streamRef, id ?? '')
// Will add: const { sendMessage, isReady } = useChat(peerRef, id ?? '')
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatPanel component and CallView integration</name>
  <files>src/components/ChatPanel.tsx, src/components/ChatPanel.test.tsx, src/components/CallView.tsx, src/pages/RoomPage.tsx</files>
  <behavior>
    - ChatPanel renders a message list from useCallStore messages
    - ChatPanel renders an input field and Send button
    - Send button is disabled when isReady is false
    - Pressing Enter in the input calls onSend with the input text and clears the input
    - Clicking Send calls onSend with the input text and clears the input
    - Empty input does not trigger onSend
    - Messages display with "You" or "Them" label based on from field
    - Message list auto-scrolls to bottom when new messages arrive
    - Input enforces max 500 character limit (per research recommendation)
    - CallView shows a chat toggle button (MessageSquare icon from lucide-react)
    - Clicking the toggle opens/closes the ChatPanel via setChatOpen in store
    - ChatPanel appears as a side panel overlay on the right side of CallView
  </behavior>
  <action>
1. Create src/components/ChatPanel.tsx:
   - Props: `{ onSend: (text: string) => void; isReady: boolean }`
   - Read `messages` from `useCallStore()`
   - Use shadcn ScrollArea for the message list container
   - Each message: show "You" (text-primary) or "Them" (text-muted-foreground) label, message text, and formatted time (HH:MM)
   - Local messages aligned right with bg-primary/text-primary-foreground bubble, remote messages aligned left with bg-muted bubble
   - Input area at bottom: shadcn Input + Button (Send icon from lucide-react)
   - `maxLength={500}` on the input
   - On Enter key (not Shift+Enter) or Send click: if input.trim() is non-empty AND isReady, call onSend(text), clear input
   - Send button disabled when !isReady or input is empty
   - Auto-scroll: useEffect watches messages.length, scrolls ScrollArea to bottom via ref.scrollTop = ref.scrollHeight
   - The panel has a header with "Chat" title and a close button (X icon) that calls setChatOpen(false)
   - Panel styling: `fixed right-0 top-0 h-full w-80 bg-background border-l border-border z-30 flex flex-col`

2. Create src/components/ChatPanel.test.tsx:
   - Test: renders message list with correct labels ("You" / "Them")
   - Test: send button disabled when isReady=false
   - Test: typing text and pressing Enter calls onSend
   - Test: empty input does not call onSend on Enter
   - Test: clicking Send button calls onSend
   - Test: input clears after send
   - Mock useCallStore to provide test messages

3. Update src/components/CallView.tsx:
   - Add new props: `sendMessage: (text: string) => Promise<void>`, `isChatReady: boolean`
   - Import `MessageSquare` from lucide-react
   - Import `ChatPanel` from './ChatPanel'
   - Read `isChatOpen` and `setChatOpen` from useCallStore
   - Add a chat toggle button in the controls bar (next to MediaControls, before hang-up button): rounded Button with MessageSquare icon
   - When isChatOpen is true, render `<ChatPanel onSend={sendMessage} isReady={isChatReady} />`
   - The toggle button should show an indicator dot (absolute positioned small circle) when there are unread messages. Track `unreadCount` with a local ref that increments on new remote messages when isChatOpen is false, resets to 0 when panel opens. Use messages.length from store to detect changes.

4. Update src/pages/RoomPage.tsx:
   - Import useChat: `import { useChat } from '../hooks/useChat'`
   - Call useChat: `const { sendMessage, isReady } = useChat(peerRef, id ?? '')`
   - Pass to CallView: `sendMessage={sendMessage} isChatReady={isReady}`
  </action>
  <verify>
    <automated>npx vitest run src/components/ChatPanel.test.tsx && npx vitest run</automated>
  </verify>
  <done>ChatPanel renders messages with local/remote distinction, handles send via Enter/button, auto-scrolls, enforces 500 char limit. CallView has chat toggle with unread indicator. RoomPage wires useChat to CallView. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of encrypted chat</name>
  <files>none</files>
  <action>
Verify end-to-end encrypted chat works across two browsers during a live video call.

What was built: End-to-end encrypted chat over DataChannel during a live 1-on-1 video call. Crypto module uses ECDH P-256 key exchange and AES-GCM-256 encryption. Chat panel with message bubbles, auto-scroll, and unread indicator.

How to verify:
1. Run `npm run dev` (starts Vite + PeerJS server)
2. Open http://localhost:5173 in Browser A — click "New Meeting"
3. Copy the room link and open in Browser B
4. In both browsers, click "Join Meeting" — verify video call connects
5. In Browser A, click the chat icon (message bubble) in the bottom controls bar — chat panel should slide open on the right
6. Type "hello from A" and press Enter — message appears as a "You" bubble (right-aligned, primary color)
7. In Browser B, click the chat icon — verify "hello from A" appears as a "Them" bubble (left-aligned, muted color)
8. In Browser B, type "hello from B" and press Enter — verify it appears in both panels
9. Send the same message "test" twice from either side — both should appear (unique IV means both encrypt/transmit fine)
10. Close the chat panel and have the other side send a message — verify the unread dot appears on the chat toggle button
11. Close both tabs — no chat data should persist (check localStorage/sessionStorage in DevTools)
12. OPTIONAL: Open DevTools Network tab during step 6-8 — verify no plaintext message content visible in any network frame
  </action>
  <verify>Human approval</verify>
  <done>User confirms encrypted chat works end-to-end: messages send/receive, bubbles display correctly, unread indicator works, no persistence after tab close.</done>
</task>

</tasks>

<verification>
- `npx vitest run` — full suite passes including ChatPanel tests
- Two-browser manual test confirms encrypted chat works end-to-end
- No plaintext in wire format
- No messages in localStorage/sessionStorage after tab close
</verification>

<success_criteria>
- Chat panel opens/closes with toggle button during active call
- Messages send and receive between two browsers with visual distinction (You/Them)
- Messages encrypted with AES-GCM using ECDH-derived key — no plaintext on wire
- Same message sent twice produces different ciphertext (unique IV)
- Chat history gone after tab close — PRIV-01 compliant
- All automated tests pass
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/04-encrypted-chat/04-02-SUMMARY.md`
</output>
